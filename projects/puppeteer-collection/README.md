# Puppeteer Collection

This directory contains miscellaneous projects that use [Puppeteer](https://github.com/puppeteer/puppeteer) to automate tasks in the browser.

Each project is a standalone Node.js project with its own `package.json` file. To run a project, navigate to its directory and run `npm install` followed by `npm start` to run the project.
